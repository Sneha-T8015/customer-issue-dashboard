# Firebase Configuration – project: helpdesk-db-75e7f
#
# EASIEST SETUP (local dev):
#   Drop the JSON key file as  ./serviceAccountKey.json
#   The app auto-detects it – no env vars needed.
#
# ALTERNATIVES (for CI / cloud platforms):
#
#   Method 1: env var pointing to key file
#   GOOGLE_APPLICATION_CREDENTIALS=/path/to/key.json
#
#   Method 2: inline JSON string (Railway, Render, etc.)
#   FIREBASE_CREDENTIALS_JSON={"type":"service_account","project_id":"helpdesk-db-75e7f",...}
#
# Flask
SECRET_KEY=change-me-to-a-random-string
PORT=5000
